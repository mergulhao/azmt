<h1>New Discipline</h1>

<%= error_messages_for :discipline %>

<% form_for(:discipline, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to 'Back', collection_url %>