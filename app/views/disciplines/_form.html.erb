<p>
  <label for="discipline_name">Nome:</label>
  <%= f.text_field :name %>
</p>
<p>
  <label for="discipline_teacher_id">Professor:</label>
  <%= f.collection_select :teacher_id, Teacher.find(:all), :id, :name %>
</p>
<p>
  <label for="discipline_classroom_id">Sala:</label>
  <%= f.collection_select :classroom_id, Classroom.find(:all), :id, :name %>
</p>
