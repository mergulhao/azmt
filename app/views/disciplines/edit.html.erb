<h2>Editando Disciplina</h2>

<%= error_messages_for :discipline %>

<% form_for(:discipline, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Atualizar"%>
  </p>
<% end %>

<br/>

<%= link_to 'Voltar', collection_url %>