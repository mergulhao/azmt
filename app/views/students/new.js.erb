<%= title_new :student %>
<%= error_messages_for :student %>

<% form_remote_for(:student, :url => collection_url, :update => "box_form") do |f| %>
  <fieldset>
    <%= render :partial => "form", :locals => { :f => f } %>
    <%= create_button_and_cancel_link_to_clear_box_form %>
  </fieldset>
<% end %>
