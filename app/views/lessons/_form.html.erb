<p>
  <label for="lesson_name">Name:</label>
  <%= f.text_field :name %>
</p>
<p>
  <label for="lesson_discipline_id">Discipline:</label>
  <%= f.collection_select :discipline_id, Discipline.find(:all), :id, :name %>
</p>
