<h1>New Classe</h1>

<%= error_messages_for :classe %>

<% form_for(:classe, :url => collection_url) do |f| %>
    <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to 'Back', collection_url %>