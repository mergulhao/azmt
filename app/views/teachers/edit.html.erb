<h2>Editando instrutor</h2>

<%= error_messages_for :teacher %>

<% form_for(:teacher, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Atualizar"%>
  </p>
<% end %>

<br/>

<%= link_to 'Voltar', collection_url %>